server.port=8081

# Base URL for the quote provider (default to this app's port so it points to the same app by default)
quote.service.base-url=http://localhost:8081
# Set to false to disable fetching a quote at application startup
quote.fetch.enabled=true
